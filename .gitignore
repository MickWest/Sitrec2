**/.DS_Store
.env
node_modules
config/config.js
config/config-install.js
.vscode/
config/shared.env
config/shared.env.mick
sitrec-videos
sitrec-cache
sitrec-upload
sitrecServer/vendor
sitrecServer/config.php
config/config.php
CLAUDE.md
CLAUDE.local.md
.wt.local.conf
.claude/
debug/
__image_snapshots__/
tests/__image_snapshots__/
/pkg/
/rust/target/
/test-results/
/playwright-cache/
/dist-standalone/
/dist/
/dist-serverless/
/.certs/
/app_build/
/app_dist/
/apps_dist/
/test-results.json
/sam2-service/venv/
/sam2-service/segment-anything-2/
/sam2-service/uploads/
/sam2-service/
# Playwright snapshot debug artifacts (Bad/Diff/Good comparison images)
*_Bad.png
*_Diff.png
*_Good.png
AGENTS.md
/tests_regression/snapshots-diffs/
/tests_regression/snapshots-baseline/
/analysis/
# BOT Bench local sweep output (benchmarks/botbench/PLAN.md) — results are
# regenerated locally; only the harness + plan are tracked
/benchmarks/botbench/results/
# Local code-review / audit reports (not part of the codebase)
/CODE-REVIEW-*.md

# Working notes: investigation write-ups, findings reports, scratch analysis.
# These are drafts that get rewritten as a task progresses — often several times
# in a session — so they are kept local rather than committed. Anything that
# earns a permanent home belongs in docs/ proper.
/docs/temp/

# Solar/lunar alignment reference photographs, for checking refraction and
# celestial positions against real images with known camera position and time.
# Third-party photographs (Wikimedia Commons) — local only, not redistributed.
/alignment-tests/
